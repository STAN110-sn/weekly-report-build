# 週報アプリ AI分析ワークフロー

このドキュメントでは、週報生成時のLLM APIコールの流れ、データの渡し方、各処理の詳細を説明します。

## 概要

週報アプリは2種類のレポートを生成します：

1. **全体向けレポート** - 個人名を含まない一般向けサマリー
2. **エグゼクティブ向けレポート** - 個人名を含む経営層向け詳細レポート

## APIコール回数

| レポート種別 | 処理内容 | 最大コール回数 |
|-------------|---------|---------------|
| 全体向け | 部署別分析 | 3回（部署数分） |
| 全体向け | 全体分析 | 1回 |
| エグゼクティブ向け | エグゼクティブ分析 | 1回 |
| エグゼクティブ向け | 個人別課題の緊急度判定 | 1回 |
| **合計** | | **最大6回** |

※ 部署別分析は日報データが存在する部署のみ実行されます

---

## 全体向けレポート生成フロー

### 1. データ整形（`_format_reports_for_analysis`）

日報データを分析用のテキスト形式に整形します。**個人名は含みません**。

```
入力: List[Dict] - Spreadsheetから取得した日報データ
出力: str - 整形されたテキスト
```

**整形フォーマット:**
```
---
日付: 2024-01-15
部署: Development
今週の作業内容: XXX
来週の作業予定: XXX
会社への要望・意見: XXX
課題・困っていること: XXX
```

### 2. 部署別分析（`_analyze_department`）

各部署（Development, Sales, Other）ごとにAPIコールを行い、部署単位のサマリーを生成します。

```
APIコール回数: 最大3回（日報データがある部署のみ）
```

**プロンプト構成:**
```
以下は{部署名}部署の今週の日報データです。
この部署の主な進捗・成果を3-5個の箇条書きで簡潔にまとめてください。
個人名は出さず、部署としての活動をまとめてください。

{整形された日報データ}

出力形式（箇条書きのみ、前置きなし）:
• 成果1
• 成果2
• 成果3
```

**出力例:**
```
• フロントエンドのリファクタリングが完了
• 新機能Aの実装を開始
• CI/CDパイプラインの改善を実施
```

### 3. 全体分析（`_analyze_overall`）

全日報データを対象に、経営者向けの週次サマリーを生成します。

```
APIコール回数: 1回
```

**プロンプト構成:**
```
あなたは社内日報を分析し、経営者向けのWeekly Reportを作成するアシスタントです。

以下は{start_date}から{end_date}までの全社員の日報データです。
このデータを分析し、以下の4つのセクションを作成してください。

【重要な注意事項】
- 個人名は絶対に出さないこと
- 部署単位（Development, Sales, Other）で言及すること
- 簡潔かつ経営判断に役立つ情報を優先すること

{全日報データ（整形済み）}

以下のJSON形式で出力してください（日本語で）:

{
    "summary": "今週の全体傾向を3-5文で要約",
    "notable_topics": [
        "複数人/部署が言及した重要トピック1",
        ...
    ],
    "issues_and_risks": [
        {
            "issue": "課題の内容",
            "severity": "高/中/低",
            "department": "関連部署"
        }
    ],
    "next_week_focus": [
        "来週の注目ポイント1",
        ...
    ]
}
```

**出力（JSON）:**
| フィールド | 説明 |
|-----------|------|
| `summary` | 今週の全体傾向（3-5文） |
| `notable_topics` | 複数人/部署が言及した重要トピック |
| `issues_and_risks` | 課題とリスク（緊急度・関連部署付き） |
| `next_week_focus` | 来週の注目ポイント |

---

## エグゼクティブ向けレポート生成フロー

### 4. データ整形（`_format_reports_for_exec_analysis`）

日報データをエグゼクティブ向けのテキスト形式に整形します。**個人名を含みます**。

```
入力: List[Dict] - Spreadsheetから取得した日報データ
出力: str - 整形されたテキスト（個人名付き）
```

**整形フォーマット:**
```
---
名前: 山田太郎
日付: 2024-01-15
部署: Development
今週の作業内容: XXX
来週の作業予定: XXX
会社への要望・意見: XXX
課題・困っていること: XXX
```

### 5. エグゼクティブ向け分析（`_analyze_for_exec`）

経営層向けの詳細分析を行います。個人名を含めた具体的な情報を出力します。

```
APIコール回数: 1回
```

**プロンプト構成:**
```
あなたは社内日報を分析し、エグゼクティブ（経営層）向けのWeekly Reportを作成するアシスタントです。

以下は{start_date}から{end_date}までの全社員の日報データです（個人名を含む）。
このデータを分析し、以下の3つのセクションを作成してください。

【重要な注意事項】
- 個人名を出してよい（エグゼクティブ向けのため）
- 「会社への要望・意見」「課題・困っていること」を中心に、重要なものを抽出すること
- 簡潔かつ経営判断に役立つ情報を優先すること

{全日報データ（個人名付き）}

以下のJSON形式で出力してください（日本語で）:

{
    "executive_summary": "今週の全体傾向を3-7文で要約（個人名を含めて具体的に）",
    "private_feedback": [
        {
            "name": "個人名",
            "feedback": "要望・意見・不満点の要点"
        }
    ],
    "action_items": [
        "経営層が対応すべきアクションアイテム1",
        ...
    ]
}
```

**出力（JSON）:**
| フィールド | 説明 |
|-----------|------|
| `executive_summary` | 個人名を含む詳細サマリー（3-7文） |
| `private_feedback` | 各個人の要望・意見・不満点 |
| `action_items` | 経営層が対応すべきアクション |

### 6. 個人別課題の緊急度判定（`_extract_individual_issues`）

各個人の「課題・困っていること」を抽出し、AIで緊急度を判定します。

```
APIコール回数: 1回（課題が存在する場合のみ）
```

**事前フィルタリング:**
以下の空回答パターンは除外されます：
- 「特になし」「特にありません」「なし」「ありません」
- 「None」「N/A」「-」「ー」
- 1文字以下の回答

**プロンプト構成:**
```
以下は社員から報告された「課題・困っていること」のリストです。
各課題について、緊急度（高/中/低）を判定してください。

【判定基準】
- 高: 業務が停滞している、期限が迫っている、複数人に影響がある、重大な問題
- 中: 進捗に影響がある、対応が必要だが緊急ではない
- 低: 軽微な問題、情報共有程度

- 山田太郎（Development）: XXXの課題
- 佐藤花子（Sales）: YYYの問題

以下のJSON形式で出力してください:

{
    "issues": [
        {
            "name": "個人名",
            "department": "部署名",
            "issue": "課題の内容",
            "urgency": "高/中/低"
        }
    ]
}
```

**出力（JSON）:**
| フィールド | 説明 |
|-----------|------|
| `name` | 報告者の名前 |
| `department` | 所属部署 |
| `issue` | 課題の内容 |
| `urgency` | 緊急度（高/中/低） |

---

## API呼び出し設定

| パラメータ | 値 | 説明 |
|-----------|-----|------|
| `model` | 環境変数 `UF_MODEL`（デフォルト: `glm-4.7-flash`） | 使用するLLMモデル |
| `temperature` | 0.3 | 出力の多様性（低め＝安定した出力） |
| `max_tokens` | 4096 | 最大出力トークン数 |
| `top_p` | 1 | 核サンプリング（デフォルト） |
| `stream` | false | ストリーミング無効 |

## リトライ機構

APIコールは指数バックオフによるリトライ機能を持っています。

| 設定 | 値 |
|-----|-----|
| 最大リトライ回数 | 3回 |
| 待機時間 | 2秒 → 4秒 → 8秒（指数バックオフ） |
| リトライ対象 | 500系エラー、タイムアウト、接続エラー |

---

## トークン使用量の追跡

各APIコールでトークン使用量が記録され、以下の情報がログ出力されます：

- 推定プロンプトトークン数（事前計算）
- 実際のプロンプトトークン数
- 完了トークン数
- 累積トークン数

トークン使用量は最終的にDriveに保存されます（`LOGS_FOLDER_ID`配下）。

---

## ワークフロー図

```
┌─────────────────────────────────────────────────────────────────┐
│                    週報生成リクエスト                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│               Spreadsheetから日報データ取得                        │
└─────────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┴───────────────┐
              ▼                               ▼
┌──────────────────────────┐    ┌──────────────────────────┐
│    全体向けレポート生成     │    │ エグゼクティブ向けレポート生成 │
└──────────────────────────┘    └──────────────────────────┘
              │                               │
              ▼                               ▼
┌──────────────────────────┐    ┌──────────────────────────┐
│ データ整形（個人名なし）    │    │ データ整形（個人名あり）    │
└──────────────────────────┘    └──────────────────────────┘
              │                               │
              ▼                               ▼
┌──────────────────────────┐    ┌──────────────────────────┐
│    部署別分析              │    │ エグゼクティブ向け分析     │
│  【APIコール×最大3回】     │    │   【APIコール×1回】       │
└──────────────────────────┘    └──────────────────────────┘
              │                               │
              ▼                               ▼
┌──────────────────────────┐    ┌──────────────────────────┐
│      全体分析              │    │ 個人別課題の緊急度判定     │
│   【APIコール×1回】        │    │   【APIコール×1回】       │
└──────────────────────────┘    └──────────────────────────┘
              │                               │
              ▼                               ▼
┌──────────────────────────┐    ┌──────────────────────────┐
│   Docs/PDF生成             │    │   Docs/PDF生成           │
│   Slack通知                │    │   Slack通知              │
└──────────────────────────┘    └──────────────────────────┘
```

---

## 関連ファイル

| ファイル | 説明 |
|---------|------|
| `ai_analyzer.py` | AI分析ロジック（本ドキュメントの主対象） |
| `main.py` | メインエントリーポイント |
| `config.py` | 設定管理 |
| `spreadsheet.py` | Spreadsheetからのデータ取得 |
| `docs_generator.py` | Google Docs/PDF生成 |
| `slack_notifier.py` | Slack通知 |
| `submission_tracker.py` | 提出状況トラッキング |
